import React from 'react';

import Sorry from './sorry';

export const Terms = () => (
  <Sorry message="Resdir is still in an early stage of development, and legal issues have yet to be resolved." />
);

export default Terms;
