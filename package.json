{
  "name": "resdir-public-monorepo",
  "description": "Resdir public monorepo",
  "private": true,
  "repository": {
    "type": "git",
    "url": "git://github.com/resdir/resdir"
  },
  "scripts": {
    "lint": "eslint .",
    "test": "run ./packages/version-range/tests run && run ./resources/js/package/tests run && run ./resources/js/dependencies/tests run && run ./resources/js/transpiler/tests run"
  },
  "devDependencies": {
    "babel-eslint": "^7.2.3",
    "eslint": "^3.19.0",
    "eslint-config-xo": "^0.18.2"
  }
}
